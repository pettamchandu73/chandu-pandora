#! /bin/sh
# $Id: postinst,v 1.1.1.1 2006/08/31 22:40:56 hari Exp $
# postinst script for msicp-0.8.7
#
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <postinst> `configure' <most-recently-configured-version>
#        * <old-postinst> `abort-upgrade' <new version>
#        * <conflictor's-postinst> `abort-remove' `in-favour' <package>
#          <new-version>
#        * <deconfigured's-postinst> `abort-deconfigure' `in-favour'
#          <failed-install-package> <version> `removing'
#          <conflicting-package> <version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package
#
# quoting from the policy:
#     Any necessary prompting should almost always be confined to the
#     post-installation script, and should be protected with a conditional
#     so that unnecessary prompting doesn't happen if a package's
#     installation fails and the `postinst' is called with `abort-upgrade',
#     `abort-remove' or `abort-deconfigure'.

case "$1" in
    configure)
        adduser --quiet --system --group --disabled-password \
                --shell /bin/false --gecos "SIP Express Router" \
                --home /var/run/msicp msicp || true

    ;;

    abort-upgrade|abort-remove|abort-deconfigure)

    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# # dh_installdeb will replace this with shell code automatically
# # generated by other debhelper scripts.
# deb_helper is not used here to avoid automatically starting msicp after
# the installation (dh_installinit 4.0 , from woody does not support
# --nostart, if you're using a newer version is safe to comment out the 
# rest of the code and add again deb_helper)

# this will be added automatically by dh_installdocs if deb_helper is 
# "enabled"
if [ "$1" = "configure" ]; then
	if [ -d /usr/doc -a ! -e /usr/doc/msicp -a -d /usr/share/doc/msicp ]; then
		ln -sf ../share/doc/msicp /usr/doc/msicp
	fi
fi
# end dh_installdocs section
# this will be automativally added by dh_installinit if deb_helper is
# "enabled
if [ -x "/etc/init.d/msicp" ]; then
	 update-rc.d msicp defaults 23 >/dev/null
	 if ! invoke-rc.d msicp restart ; then
		echo ""
		echo "msicp failed to (re)start. Perhaps your configuration requires "
		echo "additional modules (e.g. msicp-mysql-module, msicp-jabber-module "
		echo "or msicp-radius-modules). Next try to install any additional msicp"
		echo "modules you might need and then (re)start msicp by executing "
		echo "the command '/etc/init.d/msicp start|restart'."
	fi
fi
# end dh_installinit section

exit 0


